console.log('JavaScript file is linked correctly.');
alert('JavaScript file is linked correctly.');

function projectsColor() {
    var elements = document.querySelectorAll('.test');
    
    elements.forEach(function(element) {
        element.style.color = 'red'; // Change the color to red
    });
}

function resumeColor() {
    var elements = document.querySelectorAll('.test');
    
    elements.forEach(function(element) {
        element.style.color = 'red'; // Change the color to red
    });
}

function linkedinColor() {
    var elements = document.querySelectorAll('.test');
    
    elements.forEach(function(element) {
        element.style.color = 'red'; // Change the color to red
    });
}

function blogColor() {
    var elements = document.querySelectorAll('.test');
    
    elements.forEach(function(element) {
        element.style.color = 'red'; // Change the color to red
    });
}